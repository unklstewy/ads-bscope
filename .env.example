# ADS-B Scope Environment Configuration
# IMPORTANT: Copy this file to .env and configure for your environment
# Values in .env will override defaults in config.json

# =============================================================================
# Database Configuration (REQUIRED)
# =============================================================================
DB_PASSWORD=changeme_secure_password_here

# Optional database connection overrides
# ADS_BSCOPE_DB_HOST=postgres
# ADS_BSCOPE_DB_PORT=5432
# ADS_BSCOPE_DB_USER=adsbscope
# ADS_BSCOPE_DB_NAME=adsbscope

# =============================================================================
# ADS-B Data Source Configuration
# =============================================================================
# API key for online ADS-B services (e.g., ADS-B Exchange, FlightAware)
ADSB_API_KEY=your_api_key_here

# ADS-B data source (airplaneslive, adsbexchange, local, etc.)
ADSB_SOURCE=airplaneslive

# Optional: ADS-B API configuration overrides
# ADS_BSCOPE_ADSB_API_KEY=${ADSB_API_KEY}

# =============================================================================
# Telescope Configuration
# =============================================================================
# Telescope Alpaca URL (override config.json)
# ADS_BSCOPE_TELESCOPE_URL=http://seestar.local:11111

# Telescope device number (usually 0)
# ADS_BSCOPE_TELESCOPE_DEVICE_NUMBER=0

# =============================================================================
# Application Configuration
# =============================================================================
# Logging level: debug, info, warn, error
LOG_LEVEL=info

# Timezone (for logging and data processing)
TZ=UTC

# Configuration file path (if not using default)
# CONFIG_PATH=/app/configs/config.json

# =============================================================================
# Development/Testing Overrides
# =============================================================================
# Uncomment for local development (non-Docker)
# ADS_BSCOPE_DB_HOST=localhost

# Uncomment to enable debug logging
# LOG_LEVEL=debug
